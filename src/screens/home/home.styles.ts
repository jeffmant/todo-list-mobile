import { colors } from "../../styles";

export const styles = {
    container: {
        flex: 1,
        backgroundColor: colors.base.gray600,
        padding: 24,
    },
    form: {
        flexDirection: 'row',
        justifyContent: 'center',
        paddingRight: 16,
        paddingLeft: 16,
    } as any
}